import { createStyles } from 'antd-style';

export const useStyles = createStyles(({ css }) => ({
  sidebarWrapper: css`
    padding: 10px;
    width: 100%;
    height: 100%;
  `,
  logo: css`
    padding: 26px 18px 26px;
  `,
}));
